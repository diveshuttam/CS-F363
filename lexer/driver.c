#include <stdio.h>
#include "transition.h"
#include "populate.h"

Token* get_token_stream(char* filename,char* csv_filename)
{
	// char filename[40];
	// #ifndef DEBUG
	// printf("Input the file name to be compiled: \n");
	// scanf("%s",filename);
	// // #else
	// // strcpy(filename,"a.mylang");
	// // #endif

	
}

